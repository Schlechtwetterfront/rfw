/**
 * Multiply with radians values to get degrees.
 */
export const TO_DEGREES = 180 / Math.PI;

/**
 * Multiply with degree values to get radians.
 */
export const TO_RADIANS = Math.PI / 180;

export const PI_2 = Math.PI * 2;
export const LOG_2 = Math.log(2);
